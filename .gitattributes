# Normalize line endings to LF in the repo (avoids CRLF churn across machines)
* text=auto eol=lf

# Treat known binary assets as binary (no EOL conversion / diffing)
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.docx binary
*.pdf binary
