# Development / test dependencies (includes production set).
# Preferred: pip install -e ".[dev]"
# Or: pip install -r requirements-dev.txt

-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
ruff>=0.4.0
mypy>=1.10.0
faker>=25.0.0
psutil>=6.0.0
locust>=2.0.0
