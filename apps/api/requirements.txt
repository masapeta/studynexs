# Production dependencies for StudyNexs API (Noustriks product — proprietary).
# Copyright (c) 2026 Noustriks. Owner: Avinash Reddy Masapeta (ARM). See ../../LICENSE.
# Source of truth: pyproject.toml — keep these in sync when adding packages.
#
# Preferred install (editable, picks up local package):
#   pip install -e .
#
# Plain pip:
#   pip install -r requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
psycopg2-binary>=2.9.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-jose[cryptography]>=3.5.0
bcrypt>=4.0.0
redis[hiredis]>=5.0.0
httpx>=0.27.0
structlog>=24.0.0
arq>=0.26.0
edge-tts>=6.1.0
