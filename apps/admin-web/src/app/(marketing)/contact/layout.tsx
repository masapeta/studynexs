import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact — Noustriks",
  description: "Contact Noustriks for StudyNexs demos, Memory Fabric inquiries, and partnership discussions.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
